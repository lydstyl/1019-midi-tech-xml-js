'use strict';

const fs = require('fs');

const FileWriter = function(file) {
    const stream = fs.createWriteStream(file, 'utf8');
    return {
        write : stream.write.bind(stream),
        close : stream.end.bind(stream)
    }
}

module.exports = FileWriter;